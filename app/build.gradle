apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'youtubekeystore'
            keyPassword 'simsim321'
            storeFile file('/home/momo-chan/rkeystore.jks')
            storePassword 'simsim321'
        }
    }
    compileSdkVersion 22
    buildToolsVersion "22.0.1"
    defaultConfig {
        applicationId "com.projects.oleg.viewtotextureconverter"
        minSdkVersion 21
        targetSdkVersion 22
        versionCode 2
        versionName "1"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.config
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
}
